module.exports = {
  //Exportando un objeto.
  port: process.env.port || 4000, //Se le dice que el key PORT por defecto sera el puerto que se encuentre en la variable de entorno del proceso, en caso de no poderse usara el puerto 4000 por defecto.
};
